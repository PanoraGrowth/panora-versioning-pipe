# Fixture: hotfix.keyword as a list of Go regex patterns
commits:
  format: "conventional"
version:
  components:
    major:
      enabled: true
      initial: 0
    patch:
      enabled: true
      initial: 0
    timestamp:
      enabled: false
hotfix:
  keyword:
    - "^hotfix(\\(|:)"
    - "^[Hh]otfix/"
    - "URGENT-PATCH"
