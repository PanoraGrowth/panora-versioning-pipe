# Epoch + major + patch, no timestamp
commits:
  format: "conventional"

version:
  components:
    epoch:
      enabled: true
      initial: 0
    major:
      enabled: true
      initial: 0
    patch:
      enabled: true
      initial: 0
    timestamp:
      enabled: false
