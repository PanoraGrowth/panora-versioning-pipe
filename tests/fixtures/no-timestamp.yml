# Epoch + major + minor, no timestamp
commits:
  format: "conventional"

version:
  components:
    epoch:
      enabled: true
      initial: 0
    major:
      enabled: true
      initial: 0
    minor:
      enabled: true
      initial: 0
    timestamp:
      enabled: false
